[custom]
; SubConverter 外部配置：将 Mihomo YAML 模板作为 clash_rule_base 渲染。
clash_rule_base=https://raw.githubusercontent.com/Luckylos/shellcrashyaml/main/shellcrash.yaml

; 保留模板中的 rule-providers / rules，不让转换后端另行生成规则。
enable_rule_generator=false
overwrite_original_rules=false
